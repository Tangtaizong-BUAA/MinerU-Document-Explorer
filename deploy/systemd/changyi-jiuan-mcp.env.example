# Copy to /etc/changyi-jiuan-mcp.env and protect with root:changyi-kb 0640.
# Use a newly rotated MinerU credential; never commit it.
MINERU_API_KEY=
# Required for the legacy qmd --http deployment. The current home-primary
# deployment uses deploy/home/compose.yaml and project-mcp-http instead.
CYJ_MCP_BEARER_TOKEN=
