# Copy to /etc/changyi-jiuan-mcp.env and protect with root:changyi-kb 0640.
# Use a newly rotated MinerU credential; never commit it.
MINERU_API_KEY=
# Required for public HTTPS MCP access. Generate a long random value and keep
# it only in this server file and trusted Agent-client environments.
CYJ_MCP_BEARER_TOKEN=
