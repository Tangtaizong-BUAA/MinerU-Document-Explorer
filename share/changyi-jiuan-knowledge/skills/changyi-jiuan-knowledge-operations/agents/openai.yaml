interface:
  display_name: "长翼久安知识库操作"
  short_description: "增量同步并维护项目结构化知识"
  default_prompt: "Use $changyi-jiuan-knowledge-operations to incrementally sync the Skill, load the complete project main file, navigate linked evidence, and persist durable outputs and knowledge."
dependencies:
  tools:
    - type: "mcp"
      value: "changyi_jiuan_knowledge"
      description: "Authenticated Changyi Jiuan project knowledge service"
      transport: "streamable_http"
      url: "https://argonai.cn/cyj/mcp"
policy:
  allow_implicit_invocation: true
